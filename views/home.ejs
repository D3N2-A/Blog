<%- include('partials/header.ejs') %>
    <div class="container-fluid p-5">
        <h1>Home</h1>
        <p>
            <%=homeContent%>
        </p>


        <% blogPush.forEach(element=> { %>
            <h1>
                <%=element.title %>
            </h1>

            <p>
                <%= element.content.substring(0,100)+"..." %> <a href="/posts/<%=element.title %>">Read More</a>
            </p>
            <% }) %>
    </div>

    <%- include('partials/footer.ejs') %>